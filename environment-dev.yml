name: sparrow-ipc
channels:
  - conda-forge
dependencies:
  # Build dependencies
  - cmake
  - ninja
  - cxx-compiler
  # Libraries dependencies
  - flatbuffers
  - lz4-c
  - nlohmann_json
  - sparrow-devel
  - sparrow-json-reader
  # Testing dependencies
  - doctest
  # Documentation dependencies
  - doxygen
  - graphviz
