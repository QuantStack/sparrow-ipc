name: sparrow-ipc
channels:
  - conda-forge
dependencies:
  # Build dependencies
  - cmake
  - ninja
  - cxx-compiler
  # Libraries dependencies
  - flatbuffers
  - sparrow-devel >=1.1.1
  - doctest
  # Documentation dependencies
  - doxygen
  - graphviz
