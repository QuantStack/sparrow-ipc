name: sparrow-ipc
channels:
  - conda-forge
dependencies:
  # Build dependencies
  - cmake
  - make
  - ninja
  - cxx-compiler
  # TODO remove to environment-dev-external-deps.yml after handling fetch
  # Libraries dependencies
  - flatbuffers
