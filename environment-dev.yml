name: sparrow-ipc
channels:
  - conda-forge
dependencies:
  # Build dependencies
  - cmake
  - ninja
  - cxx-compiler
  # Libraries dependencies
  - flatbuffers
  - lz4
  - nlohmann_json
  - sparrow-devel >=1.1.2
  # Testing dependencies
  - doctest
  # Documentation dependencies
  - doxygen
  - graphviz
