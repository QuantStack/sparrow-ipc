name: sparrow-ipc
channels:
  - conda-forge
dependencies:
  # Build dependencies
  - cmake
  - make
  - ninja
  - cxx-compiler
  # Libraries dependencies
  - flatbuffers
  - sparrow
  # Tests
  - doctest
